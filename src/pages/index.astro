---
import Default from "@/layouts/Default.astro";
import { uniqueYears } from "@/lib/artworks";

const earliestYear = Math.min(...[...uniqueYears].map((i) => Number(i)));
const latestYear = Math.max(...[...uniqueYears].map((i) => Number(i)));
---

<Default
  title="Artwork Archive | IntoEkinZone"
  description={`Artwork from ${earliestYear} to ${latestYear}`}
>
  <body class="p-8">
    <header class="mb-4">
      <h1 class="text-6xl text-mustard font-bold">Artwork Archive</h1>
    </header>

    <main>
      <ul class="space-y-2">
        {
          uniqueYears.map((year) => (
            <li>
              <a class="text-2xl hover:underline" href={`/${year}`}>
                {year}
              </a>
            </li>
          ))
        }
      </ul>
    </main>
  </body>
</Default>
